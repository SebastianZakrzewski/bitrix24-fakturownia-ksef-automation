---
description: Docs selection policy — read only relevant /docs before coding; never implement from memory
alwaysApply: true
---

# Docs Selection Policy

Before coding, inspect `.cursor/rules` and the `/docs` files relevant to the **current task**.

Do **not** read all `/docs` by default unless the task is broad, architectural, or touches multiple core areas.

Use category rules in `.cursor/rules/docs-selection/` (matched by task area or open file globs).

## Always read (every implementation task)

- `.cursor/rules/*`
- `/docs/decision-log.md`

## If unclear

1. Do not guess which docs apply.
2. Propose the relevant docs to inspect.
3. Ask for approval before coding.

Forbidden:
- implement from memory
- proceed without inspecting required docs for the task category

## Category index

| Task area | Rule file |
|-----------|-----------|
| DB, migrations, repositories | `10-db-persistence.mdc` |
| Lifecycle, statuses, state machine | `20-lifecycle-statuses.mdc` |
| DTO, API, controllers | `30-dto-api-controllers.mdc` |
| Validation, mapping, InvoiceDraft | `40-validation-mapping.mdc` |
| Bitrix24 module | `50-bitrix24.mdc` |
| Fakturownia integration | `60-fakturownia.mdc` |
| Testing | `70-testing.mdc` |
| Main invoice workflow / use case | `80-invoice-workflow.mdc` |
| Tech Lead audit / review / deviation check | `90-tech-lead-audit.mdc` + `.cursor/rules/audit/*` |

Broad or multi-area tasks: combine doc lists from all matching categories (dedupe paths).

**Audit tasks:** read `07-tech-lead-audit-policy.mdc`; review only unless user asks to fix.
